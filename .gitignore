target/
*.log
.idea/
*.iml
.DS_Store

# frontend (React + Vite) — built output & deps are not committed
src/main/frontend/node_modules/
src/main/frontend/dist/
src/main/frontend/*.tsbuildinfo
# .npmrc holds a private-registry auth token — never commit it
src/main/frontend/.npmrc
.npmrc
